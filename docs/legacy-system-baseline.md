# Legacy System Baseline

The initial baseline is copied from `FiinGroup.Jarvis/aidlc-docs/inception/reverse-engineering/`.

It describes the Jarvis CMS/Goline internal management system: a static HTML/CSS/JavaScript frontend, Node.js/Express backend, MySQL/MariaDB data model, workers, SMTP, TFS/NTLM integration, and StoneHub chatbot proxy.

## Revalidation rule

Counts, endpoint inventories, security findings, dependency versions, and deployment assumptions must be rechecked against the current legacy repository before being used as migration commitments.

## Source repository

`D:\DEV\FiinGroup.Jarvis`
